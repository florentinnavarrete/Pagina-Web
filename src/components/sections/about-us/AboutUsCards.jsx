/**
 * Colección de tarjetas que muestran los servicios que se ofrecen.
 *
 * @returns {React.JSX.Element}
 * @constructor
 */
function AboutUsCards({ children }) {
    return (
        <div id="about-us-cards">
            {children}
        </div>
    )
}

export default AboutUsCards;